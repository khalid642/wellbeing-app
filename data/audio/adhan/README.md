# Bundled Adhan Audio Directory

Place default/bundled adhan audio files in this directory.

Supported formats:
- MP3 (`.mp3`)
- OGG (`.ogg`)
- WAV (`.wav`)
- OPUS (`.opus`)
- M4A (`.m4a`)
- FLAC (`.flac`)

Custom adhans uploaded by the user are stored in:
`~/.config/wellbeing-app/audio/adhan/`
